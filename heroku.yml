setup:
  config:
    NODE_ENV: production
build:
  docker:
    web: Dockerfile
  config:
    WORKDIR: app
    API_URL: "https://rails-vue3cli-api.herokuapp.com"
run:
  web: npm run build && npm run start
